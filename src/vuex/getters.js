export const getId = state => {
  return state.id;
};

export const getDirId = state => {
  return state.dirId;
};

export const getAddFolder = state => {
  return state.addFolder;
};

export const getNewFolderName = state => {
  return state.newFolderName;
};

export const getRefreshFileList = state => {
  return state.refreshFileList
};

export const getShowDelete = state => {
  return state.showDelete
};

export const getSearch = state => {
  return state.search
};

export const getSearchText = state => {
  return state.searchText
};
